<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.RandomUser" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background. -->
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <!-- Use windowSplashScreenAnimatedIcon to add either a drawable or an
        animated drawable. One of these is required. -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_icon
        </item> <!--TODO : Replace with required splash icon-->
        <!-- Set the theme of the Activity that directly follows your splash screen. -->
        <item name="postSplashScreenTheme">@style/Theme.RandomUser</item>
    </style>
</resources>